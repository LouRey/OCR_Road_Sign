# Core App
flask
streamlit==0.87.0
tqdm
pandas==1.3.5
numpy==1.19.5

# Kedro
kedro==0.17.7
kedro-datasets==0.9.1

# OCR
pytesseract==0.3.13
scikit-image
pyclipper
python-bidi
Shapely

# ATTENTION: easyocr installe via pip --no-deps (voir Dockerfile)

# Detection objets
# ATTENTION: ultralytics et ultralytics-thop installes via pip --no-deps (voir Dockerfile)
matplotlib
psutil
Pillow
torchvision

# Optionnel
scipy
python-slugify

# Utilitaires
PyYAML
requests

# Fix compatibilite Kedro / Altair
typing-extensions==3.10.0.2
importlib-metadata<5.0
