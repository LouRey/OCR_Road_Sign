# Core App
flask
streamlit
tqdm
pandas==2.0.3
numpy==1.24.4

# Kedro
kedro==0.19.5
kedro-datasets==1.6.0

# OCR
pytesseract==0.3.13
scikit-image

# ATTENTION: easyocr installé via pip --no-deps (voir Dockerfile)

# Détection objets
# ATTENTION: ultralytics et ultralytics-thop installés via pip --no-deps (voir Dockerfile)

# Optionnel
scipy
python-slugify

# Utilitaires
PyYAML
requests

# Fix incompatibilité Altair & kedro
typing-extensions>=4.10.0
packaging==24.0
importlib-metadata<8.0
